<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>Sohbet</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style> 
        body { font-family: 'Inter', sans-serif; background-color: #f0f2f5; font-size: 0.9rem; padding-top: 80px; } 
        .bubble { max-width: 75%; padding: 10px 15px; border-radius: 15px; margin-bottom: 8px; font-size: 0.9rem; position: relative; }
        .sent { background-color: #d1e7dd; margin-left: auto; border-bottom-right-radius: 0; color: #0f5132; }
        .received { background-color: #ffffff; border: 1px solid #dee2e6; margin-right: auto; border-bottom-left-radius: 0; }
        .time { font-size: 0.65rem; color: #6c757d; display: block; text-align: right; margin-top: 4px; }
    </style>
</head>
<body>

<!-- GÜNCEL NAVBAR (Standart) -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top py-2 shadow-sm">
    <div class="container">
        <a class="navbar-brand fw-bold text-warning fs-5" href="/">UstaBul</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav"><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="mainNav">
            <ul class="navbar-nav ms-auto align-items-center gap-3">
                <li class="nav-item"><a class="nav-link" href="/">Anasayfa</a></li>
                <li class="nav-item"><a class="nav-link text-warning" href="/worker-panel">İş Pazarı</a></li>
                
                <% if (user) { %>
                    <li class="nav-item"><a class="nav-link" href="/inbox">Mesajlar</a></li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            Profilim
                            <!-- BİLDİRİM ROZETİ BURADA -->
                            <% if(locals.totalNotificationCount > 0) { %> 
                                <span class="badge bg-danger rounded-pill"><%= totalNotificationCount %></span> 
                            <% } %>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow-sm border-0">
                            <li><h6 class="dropdown-header text-muted text-uppercase small fw-bold">İşveren</h6></li>
                            <li>
                                <a class="dropdown-item d-flex justify-content-between align-items-center" href="/profile">
                                    <span><i class="bi bi-briefcase"></i> İşveren Paneli</span>
                                    <% if(locals.empNotificationCount > 0) { %>
                                        <span class="badge bg-danger rounded-pill ms-2"><%= empNotificationCount %></span>
                                    <% } %>
                                </a>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li><h6 class="dropdown-header text-muted text-uppercase small fw-bold">Usta</h6></li>
                            <li>
                                <a class="dropdown-item d-flex justify-content-between align-items-center" href="/worker/profile">
                                    <span><i class="bi bi-tools"></i> İşçi Paneli</span>
                                    <% if(locals.workerNotificationCount > 0) { %>
                                        <span class="badge bg-danger rounded-pill ms-2"><%= workerNotificationCount %></span>
                                    <% } %>
                                </a>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item text-danger fw-bold" href="/logout"><i class="bi bi-box-arrow-right"></i> Çıkış Yap</a></li>
                        </ul>
                    </li>
                <% } else { %>
                    <li class="nav-item ms-2"><a class="nav-link text-white" href="/login">Giriş</a></li>
                    <li class="nav-item"><a class="btn btn-warning btn-sm fw-bold text-dark px-4 rounded-pill" href="/register">Kayıt Ol</a></li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            
            <!-- Üst Başlık ve Geri Butonu -->
            <div class="d-flex align-items-center mb-3">
                <a href="/inbox" class="btn btn-light btn-sm border shadow-sm me-3 rounded-circle" style="width: 32px; height: 32px; display: flex; align-items: center; justify-content: center;">
                    <i class="bi bi-arrow-left"></i>
                </a>
                <h5 class="fw-bold m-0"><%= targetName %> ile Sohbet</h5>
            </div>

            <!-- Sohbet Kutusu -->
            <div class="bg-white p-3 rounded shadow-sm mb-3 border" style="height: 65vh; overflow-y: auto;" id="chatBox">
                <% if(history.length === 0) { %> 
                    <div class="text-center text-muted mt-5 small">
                        <i class="bi bi-chat-dots fs-1 d-block mb-2 opacity-50"></i>
                        Sohbeti başlatın...
                    </div> 
                <% } %>
                
                <% history.forEach(msg => { %>
                    <div class="bubble <%= msg.senderId == myId ? 'sent' : 'received' %>">
                        <%= msg.content %>
                        <span class="time">
                            <%= msg.date.split(' ')[1] ? msg.date.split(' ')[1].substring(0, 5) : msg.date %>
                            <%= msg.senderId == myId ? '✓' : '' %>
                        </span>
                    </div>
                <% }) %>
            </div>

            <!-- Mesaj Gönderme Formu -->
            <form action="/send-message" method="POST">
                <input type="hidden" name="senderId" value="<%= myId %>">
                <input type="hidden" name="receiverId" value="<%= targetId %>">
                <input type="hidden" name="role" value="<%= myRole %>">
                
                <div class="input-group shadow-sm">
                    <input type="text" name="content" class="form-control border-0" placeholder="Bir mesaj yazın..." required autocomplete="off" style="padding: 12px;">
                    <button class="btn btn-primary px-4 fw-bold">
                        <i class="bi bi-send-fill"></i>
                    </button>
                </div>
            </form>

        </div>
    </div>
</div>

<!-- BU SCRIPT EKLENDİĞİ İÇİN ARTIK MENÜ AÇILACAK -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script> 
    // Sayfa açılınca en alta kaydır
    const box = document.getElementById('chatBox'); 
    box.scrollTop = box.scrollHeight; 
</script>
</body>
</html>